const firstArray = new Array("Mama", "doesn't", "know", "about");
const secondArray = new Array("what", "we", "have", "done", "last", "night");
const result = firstArray.concat(secondArray);

console.log(result);
